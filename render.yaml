services:
  - type: web
    name: ai-gym-trainer
    env: docker
    plan: free
    dockerContext: .
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: USE_BACKEND_CAMERA
        value: "0"
      - key: SEND_WS_FRAMES
        value: "0"
